name = "ai-growthos-frontend"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"

[build]
command = "npm install && npm run build"

[[pages.routes]]
pattern = "/api/*"
# API calls are proxied to the backend worker
