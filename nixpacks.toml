[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["cd backend && npm install --include=dev", "cd frontend && npm install --include=dev"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "node backend/src/index.js"
